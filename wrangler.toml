name = "braindrop-proxy"
main = "cloudflare-worker-proxy.js"
compatibility_date = "2024-12-01"

# Routes — uncomment and edit if using a custom domain
# routes = [
#   { pattern = "api.braindrop.app/*", zone_name = "braindrop.app" }
# ]

# Secrets (set via: npx wrangler secret put <NAME>)
# These are NOT stored in this file — they live in Cloudflare's encrypted vault.
# Required secrets:
#   ANTHROPIC_API_KEY
#   GEMINI_API_KEY
#   GROQ_API_KEY
#   OPENAI_API_KEY
#   DEEPSEEK_API_KEY
#   MISTRAL_API_KEY
